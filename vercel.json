{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "prisma generate && prisma db push --skip-generate --accept-data-loss && npm run db:seed && next build",
  "crons": [
    {
      "path": "/api/ical/sync",
      "schedule": "0 4 * * *"
    }
  ]
}
